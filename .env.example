REDIS_PASSWORD=change-me

# S3 bucket persisting report PDFs (empty = local filesystem).
ENCELADUS_REPORTS_BUCKET=
ENCELADUS_REPORTS_PREFIX=

CORS_ORIGINS=*
# Optional: use a specific SIDRA year instead of the latest estimate.
# IBGE_POPULATION_PERIOD=2025
# Optional: maximum size of the persistent DataSUS cache (default: 5 GiB).
# ENCELADUS_DATASUS_CACHE_MAX_BYTES=5368709120
